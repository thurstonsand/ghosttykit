import type { ExtensionAPI } from "@earendil-works/pi-coding-agent";

export default function piPaste(_pi: ExtensionAPI): void {
	// Implementation will be extracted from the current pi-paste-file extension.
}
