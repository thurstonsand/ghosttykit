set shell := ["bash", "-euo", "pipefail", "-c"]

_default:
    just --list

fmt:
    npm run fmt

fmt-check:
    npx --no-install biome format .

lint:
    npm run lint

typecheck:
    npm run typecheck

check:
    npm run check

build: typecheck

install-deps:
    npm install
