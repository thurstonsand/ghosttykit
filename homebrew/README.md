# Homebrew

Homebrew packaging will live here during extraction.

The formula should install `gty` and `ghosttykitd`, expose a `brew services` user service for `ghosttykitd`, and codesign binaries where practical with ad-hoc fallback for source builds.
